#include <iostream>
#include <vector>
#include <algorithm>

int main(){
    int cantidad;
    std::vector<float> estaturas;

    std::cout<<"Ingrese la cantidad de estaturas que desea ingresar \n";
    std::cin >> cantidad;
    for (int i = 0; i < cantidad; i++){
        std::cout << "Ingrese la estatura #" <<i+1 <<"\n";
        std::cin >> 
    }
    


  return 0;
}